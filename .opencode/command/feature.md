---
description: Start a new Clipora feature following the vertical-slice workflow.
agent: build
---

Load the `feature` skill.

1. Clarify the feature: outcome, acceptance criteria, non-goals, and which modules are affected.
2. Follow the 8-step vertical slice workflow (pure function/model first → unit tests → core + integration → UI → manual checks → docs).
3. Follow the `docs-sync` skill when updating docs.

$ARGUMENTS
